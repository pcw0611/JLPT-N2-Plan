# JLPT 일일 마감 리포트 운영 규칙

사용자가 `오늘 끝`, `마무리`, `여기까지` 또는 이에 준하는 표현을 하면 별도 요청이 없어도 그날의 최종 리포트를 제공한다.

## 필수 항목

1. 확인된 학습시간과 미계시 활동
2. 완료한 강의·Anki·복습·문제 수
3. 시험별 점수와 출처 구분
   - 자체 제작
   - 교재·강의
   - 공식 문제집·모의고사
4. 어휘·문법·독해·청해 및 세부 유형별 점수·내부 등급·신뢰도
5. 즉시 선택형, 자유회상, D+1·D+3·D+7 결과 분리
6. 합격 가능성
   - 현재 응시 시 추정 범위
   - 시험일까지 예상
   - 전일 대비 변화와 변화 이유
   - 수치가 유지되면 `유지`로 명시
7. 새 오답, 반복 약점, 졸업한 약점
8. 다음 학습 시작 순서와 판정 기준

## 문제 유형별 추적

- 문항별 해설 첫머리에 `영역 → 문제 유형 → 세부 유형`을 표시한다. 예: `청해 → 포인트이해(ポイント理解) → 가격·할인 대상`. 마지막 세부 유형은 학습용 분류다.

각 세부 유형마다 다음 값을 표시한다.

- 누적 정답률과 누적 문항 수
- 최근 10문제 정답률
- D+1·D+3·D+7 유지율
- 모름률과 선택 후 오답률
- 평균·중앙 풀이시간
- 훈련용 권장시간과의 차이
- 이전 리포트 대비 상승·유지·하락

어휘는 한자 읽기·표기·어구 형성·문맥 규정·유의표현·용법, 문법은 문법형식 판단·문장 배열·담화 문법과 기초 활용, 독해는 단문·중문·통합이해·주제이해·정보검색, 청해는 과제이해·포인트이해·개요이해·즉시응답·통합이해로 구분한다.

- 유형당 5문제 미만은 `방향 신호`로만 표시한다.
- 유형당 10문제 이상이고 최소 3개 날짜에 걸친 결과가 있으면 내부 등급 산정에 본격 반영한다.
- 누적률이 최근 성장을 가리지 않도록 최근 10문제 정답률을 함께 표시한다.
- 청해는 오디오 길이와 오디오 종료 후 결정시간을 분리한다.
- 문항별 권장시간은 공식 제한이 아니라 N2 전체 시험시간에 맞춘 훈련 기준임을 표시한다.
- 시험 영역 스테이터스와 기억·학습 효율 스테이터스를 별도 표로 분리한다.

## 해석 원칙

### 홈페이지 날짜별 표시 기준 (2026-08-28 갱신)

- 영역은 어휘 → 문법 → 독해 → 청해, 세부 유형은 홈페이지의 `lib/report-taxonomy.json` 순서로 고정한다. DB 삽입 순서·평가 순위에 따라 행 순서를 바꾸지 않는다.
- 모든 날짜에 등록된 동일 유형을 표시한다. 당일 미응시는 `未実施`이며 0점이 아니다. 당일 정답 수에 누적 표본 수를 분모로 붙이지 않는다.
- 당일 결과, 선택 날짜까지의 누적 결과, 내부 참고 평가를 구분한다. 미래 날짜의 응답을 과거 누적값에 포함하지 않는다.
- 시험 합계만 있고 문항 상세가 없는 자료는 유형을 추정하지 않으며, 합계와 유형별 표가 다른 이유 및 문항 수를 명시한다.
- 청해 평균 시간은 오디오 종료 후 선택 시간, 다른 유형은 문항 전체 시간이다. 미계시·알려진 계시 오류는 평균에서 제외하고 유효 표본 수를 표시한다. 음성 오류 문항은 유형별 성적·속도에서 제외한다.
- 기록이 있다는 이유로 학습일 종료·전체 활동 계상으로 표시하지 않는다. 기록 여부와 실제 종료는 별개다.
- 과거 날짜의 복습 예정은 해당 날짜까지의 문제에서 등록된 계획으로 표시한다. 현재 대기 건수 또는 과거 시점의 완료 상태라고 표현하지 않는다.
- 전체 날짜 재동기화는 원본 점수·시간·확률을 변경하지 않고 동일한 집계 규칙으로 홈페이지 데이터만 재작성한다.

### 공통 해석

- 자체 제작 소표본 시험 하나로 합격률을 크게 변경하지 않는다.
- 공식 문제집과 시간제 실전 모의고사에 가장 높은 가중치를 준다.
- `모름`, `선택 후 오답`, `시간 종료 미응답`을 분리한다.
- 선택한 오답이 전달되면 함정 유형을 추정하되, 확인된 사실과 추정을 구분한다.
- 학습시간이나 결과가 누락되면 추정하지 않고 `미계시`로 표시한다.
- 같은 세부 유형을 여러 날짜에 반복 측정하고 D+1 결과가 있을 때 내부 등급의 신뢰도를 높인다.
