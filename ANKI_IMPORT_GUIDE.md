# 일본어 어감 묶음 Anki 가져오기

대상 파일: `anki_japanese_nuance_words_2026-08-26.tsv`

## 권장 필드 순서

1. 표현
2. 읽기
3. 한국어 뜻
4. 구분 포인트
5. 일본어 예문
6. 예문 해석
7. 태그

## 가져오기 설정

- 구분자: 탭
- 첫 줄: 필드 이름으로 사용
- HTML 허용: 꺼도 무방
- 중복 기준: 표현
- 마지막 열을 태그 필드에 연결

## 카드 권장 구성

앞면:

```text
{{표현}}
```

뒷면:

```text
{{읽기}}
{{한국어 뜻}}
{{구분 포인트}}

{{일본어 예문}}
{{예문 해석}}
```

## 학습 순서

- 1일차: `어감::혼란_눈`, `어감::완전_상쾌` 중 5~8장
- 2일차: `어감::마침내`와 전날 잔여 카드
- 3일차: `어감::의도_실수`
- 4일차: `어감::행동방식`
- 5일차: `어감::상태`

기존 만기 복습을 완료한 뒤 신규 카드를 연다. 이 파일의 25장을 한 번에 모두 학습하지 않는다.

## 기본 2필드 카드 유형을 사용할 때

기본 카드에서 앞면과 뒷면에 일본어 표현만 반복되어 보이면 `anki_japanese_nuance_basic_2field_2026-08-26.tsv`를 사용한다.

- 열 1 `Front` → 앞면
- 열 2 `Back` → 뒷면
- 열 3 `Tags` → 태그
- 가져오기 화면에서 **HTML 허용**을 켠다.

이 파일은 뜻, 구분 포인트, 예문과 번역을 하나의 `Back` 필드에 합쳐 두었으므로 별도 카드 템플릿을 만들 필요가 없다.
