# JLPT N2 Codex project rules

이 프로젝트의 최우선 목표는 일본어 회화 일반 학습이 아니라 2026-12-06 JLPT N2 합격과 110점 이상이다.

## 새 작업 시작

- 가장 먼저 `PROJECT_GUIDE.md`를 끝까지 읽어 고정 운영 원칙을 확인한다.
- 이어서 `HANDOFF.md`를 끝까지 읽고 현재 상태와 다음 행동을 확인한다.
- `database/jlpt_learning.db`, `JLPT_STUDY_LOG.md`, `JLPT_ERROR_NOTE.md`, `JLPT_REPORTING_STANDARD.md`를 기록 기준으로 사용한다.
- 사용자가 완료했다고 명시하지 않은 학습이나 시험을 추정해서 기록하지 않는다.
- `references/`는 원본 학습자료로 취급하고 수정하지 않는다.

## 퀴즈

- 시험·퀴즈 제작, 변환, 수정, 검토를 시작하기 전에 `docs/JLPT_EXAM_AUTHORING_STANDARD.md`를 읽고 적용한다. 공식 원문 대조, 정답의 유일성, 채점 일치, 실행 검증 범위를 근거로 남긴다.
- 퀴즈는 대화 내 인터랙티브 시각화로 제공한다.
- 문제 화면은 일본어만 사용하고, 전 문항 선택형 및 `分からない` 선택지를 제공한다.
- `開始する` 이후 타이머를 시작하고 제출 후 자동 채점한다.
- 해설은 채점 후 한국어로 제공하며 문제 원문, 번역, 정답, 접속·의미, 함정, 유사 문형 차이, 추가 예문, 다음 복습일을 포함한다.
- 각 문항 해설의 첫머리에 `영역 → 문제 유형 → 세부 유형` 카테고리를 표시한다. 예: `청해 → 과제이해(課題理解) → 담당자·행동 순서`. 세부 유형은 학습용 분류이며 공식 문제 유형과 구분한다.
- 공식 문제집 원문 응시의 청해는 제공되는 공식 음원을 우선하고, 자체 제작·변형 연습에는 일본어 TTS를 사용한다. 음원 종류와 출처를 구분하며 답변 전에는 대본을 공개하지 않는다. TTS는 가능한 경우 화자별 음색을 구분하고 재생 제한을 적용한다.
- 결과에는 유형별 정답률, 소요시간, 오답, `分からない`, 미응답과 사용자가 선택한 답을 포함한다.

## 기록과 사이트

- 시험 결과는 공식 시험·공식 모의고사·자체 제작 시험을 구분해 기록한다.
- 오답 및 `分からない`는 D+1, D+3, D+7 복습 대상으로 관리한다.
- 홈페이지 소스는 `jlpt-calendar-site/`, 공개 주소는 `https://jlpt-study-calendar.pcwww.chatgpt.site/`이다.
- 홈페이지 변경 또는 배포 시 프로젝트의 Sites 설정과 기존 공개 주소를 보존한다.
- 비밀키, `.env`, `.sync-secret`, 빌드·캐시 산출물은 저장소에 추가하지 않는다.

## 사용자 상호작용

- 사용자가 `끝`이라고 했을 때 무엇이 끝난 것인지 문맥으로 판단한다. 학습일 종료는 `오늘 끝`, `오늘 학습 종료`처럼 명확한 신호로 처리한다.
- 학습 종료 보고에는 학습시간, 완료 항목, 시험 결과, 유형별 변화, 강점·약점, 합격 가능성 변화와 다음 일정을 포함한다.
- 사용자가 `동기화` 또는 `동기화해줘`라고 요청하면 즉시 `python sync.py`(Mac은 `python3 sync.py`)를 실행해 원격 저장소 및 캘린더 사이트와 동기화하고 완료 상태를 보고하며 Anki 동기화(단축키: y)를 상기시킨다.
