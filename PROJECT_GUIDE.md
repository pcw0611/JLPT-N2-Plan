# JLPT N2 플랜 — 프로젝트 가이드

## 프로젝트 목적

이 프로젝트의 목적은 일반 회화 능력이나 일본어를 우아하게 공부하는 것이 아니라 **2026-12-06 JLPT N2 합격과 110점 이상**이다. 모든 일정·문제·기록·추천은 시험 합격에 얼마나 기여하는지를 우선 기준으로 삼는다.

## 사용자 학습 성향

- 사용자는 장시간 고강도 학습이 가능하고 필요하면 하루 10~12시간도 투자할 의지가 있다.
- 문법을 빠르게 관통해 “단어만 알면 지문 구조를 읽을 수 있다”는 자신감을 먼저 만들고 싶어 한다.
- 단순 격려보다 수치, 정답률, 시간, 유형별 변화, 오답 원인을 선호한다.
- 독해와 논리적 오류 감지가 상대적 강점이다.
- 청해 과제이해·행동 순서와 신규 문형의 접속·의미 구별은 집중 보완 대상이다.
- 같은 문제의 정답 암기보다 새 문맥에서의 판별력을 원한다.
- 목표가 선택형 JLPT 합격이므로 주관식 문제는 원하지 않는다.

## 학습 전략

1. N3 기반 문법을 빠르게 완성한다.
2. N2 문법을 강의 순서대로 관통한다.
3. 강의 직후 짧은 선택형 확인시험으로 당일 재인을 확인한다.
4. 오답과 `分からない`는 D+1, D+3, D+7에 새 문맥으로 변형해 다시 묻는다.
5. 독립 표본과 즉시 교정을 구분한다.
6. 청해는 매일 20~30분 유지한다.
7. Anki는 만기 복습 우선, 새 카드는 다음날 부하를 보며 조절한다.
8. 일정은 데이터에 따라 조정하되 하루 결과 하나에 과잉 반응하지 않는다.

### N2 문법 강의와 Anki

- 다락원 N2 문법 강의를 완료하면 해당 범위의 문형을 문형당 1장씩 기존 N2 문법 Anki 덱에 당일 추가한다.
- 앞면은 문형, 뒷면은 한국어 뜻·접속·핵심 의미·일본어 예문과 번역·유사 문형 차이·시험 신호를 포함한다.
- 카드 표시는 2026년 8월 문법 001~025 카드의 구획형 레이아웃을 표준으로 삼고, 뒷면에 `접속·핵심 의미·예문 1·예문 2·구별 포인트·시험 신호`를 같은 순서와 스타일로 넣는다.
- 카드 생성은 학습 완료나 학습시간으로 계산하지 않는다. 사용자가 실제로 보았다고 확인한 강의 범위만 추가한다.

## 퀴즈 제품 규격

- 모든 퀴즈는 오른쪽 인앱 브라우저에서 사용할 웹 기반 시각형 인터페이스로 제공한다.
- 문제·선택지·버튼 등 시험 화면은 일본어만 사용한다. 실제 시험과 동일하게 시험 문제 풀이 화면(제출 전)에는 한자에 후리가나를 절대 노출하지 않는다.
- 해설은 제출 후 한국어로 제공하며, 문제 원문·정답·예문의 한자에 학습용 후리가나(<ruby> 태그)를 완비해 제공한다.
- 전 문항 선택형이며 자유입력·주관식은 사용하지 않는다.
- 모든 문항에 `分からない`를 제공한다.
- `開始する`를 눌렀을 때 타이머가 시작된다.
- 제출 후 자동 채점한다.
- 실제 JLPT의 문자·어휘, 문법형식 판단, 문장 배열, 글의 흐름, 단문·중문·장문 독해, 청해 문제 유형을 우선한다.
- 문법 용어 자체를 맞히는 문제는 학습용 비공식 유형으로 분리한다.
- 선택지의 문법적 유일성을 출제 전에 검사한다. 복수 정답 가능성이 있으면 문맥을 고치거나 제외한다.
- 모든 문제 화면에는 사용자가 무엇을 골라야 하는지 일본어 지시문을 명시한다. 유형명만 표시하거나 문장을 대뜸 제시하지 않는다. 예: `意味が最も近いもの`, `（　）に入るもの`, `★に入るもの`, `音声を聞いて答える`.
- 유의표현 문제는 비교 대상 단어를 지시문에 직접 적거나 문장 안에서 명확히 표시한다.
- 결과에는 총점, 정답률, 시간, 영역별·유형별 성적, 문항별 시간, 오답, `分からない`, 미응답, 선택한 답을 담는다.

### 시험 순서·셔플·지구력

- 만기 복습 문항이 많을 때는 사용자가 분할을 요청하지 않는 한 **한 세션의 연속 시험**으로 제공한다. 중간 채점·정답 공개 없이 끝까지 응시해 시험 지구력을 기른다.
- 포함된 영역은 실제 JLPT의 큰 순서에 가깝게 `문자·어휘 → 문법형식 판단 → 문장 배열 → 글의 문법 → 독해 → 청해`로 배치한다. 없는 영역은 억지로 만들지 않는다.
- 큰 유형 순서는 유지하되 같은 유형 안에서는 원강의 순서, 문형 번호, 원시험, D+1/D+3 순서가 힌트가 되지 않도록 문항을 셔플한다.
- 선택지도 시험 시작마다 셔플하고 `分からない`만 항상 마지막에 고정한다. 정답 위치 분포가 한쪽으로 치우치지 않았는지 검사한다.
- 지구력 시험 결과에는 전반부와 후반부의 정답률·평균시간을 따로 표시해 후반 집중력 저하를 확인한다.

### 퀴즈 구현 재사용·사용량 절약

- 일상적인 확인시험과 만기 복습은 `quiz_templates/`와 이미 검증된 퀴즈 소스를 우선 재사용한다. 2026-08-31의 연속시험 구현 기준 소스는 `quiz_sites/due-review-20260831-44/`다.
- 문항 데이터·시험 제목·복습 큐 연결만 바꾸는 것을 우선하며, 매 시험마다 새 사이트 구조·디자인·공유 이미지를 다시 만들지 않는다.
- 새 Sites 프로젝트, 새 공개 주소, ImageGen 공유 이미지, 별도 데이터베이스는 사용자가 명시적으로 요청했거나 기존 실행면으로 기능을 충족할 수 없을 때만 만든다.
- 내부 학습용 일회성 시험에는 소셜 미리보기 이미지가 필요하지 않다. 기존 배포면을 재사용할 수 있으면 새 배포를 만들지 않는다.
- 빌드·배포는 전체 문항과 기능을 먼저 완성·검증한 뒤 원칙적으로 한 번만 한다. 메타데이터나 장식 때문에 중복 빌드·배포하지 않는다.
- 반복 사용이 확인된 기능은 시험별 복사본을 늘리기보다 하나의 재사용 가능한 시험 실행기로 합친다.

## 청해 규격

- 일본어 TTS를 사용한다.
- 문제를 풀기 전에는 대본과 번역을 공개하지 않는다.
- 화자가 여러 명이면 목소리를 충분히 구분한다.
- 화자 표기를 TTS가 읽지 않게 발화별로 분리한다.
- 재생 횟수를 기록하고 문제 성격에 따라 1회 제한을 적용한다.
- 오디오 길이와 선택 결정 시간을 가능하면 분리한다.
- TTS 발음 오류 가능성이 있으면 사용자의 청취 실수로 단정하지 않는다.

## 채점·오답 처리

- 공식 시험, 공식 모의고사, 준공식 자료, 자체 제작 문제를 구분한다.
- 자체 제작 결과를 JLPT 환산점수처럼 표현하지 않는다.
- 오답, `分からない`, 미응답, 시간 초과를 서로 구분해 기록한다.
- 오답 사슬은 새 문맥의 D+7 문항을 맞히면 졸업 처리한다. 정답에는 새 복습 일정을 만들지 않는다.
- D+7까지 가기 전의 정답은 이미 잡힌 다음 간격을 유지하되, 오답·`分からない`만 새 D+1·D+3·D+7 사슬을 만든다.
- 같은 약점의 활성 복습 사슬이 겹치면 중복 문항을 계속 늘리지 않고 하나의 최신 사슬로 통합한다.
- 모호한 문항은 사용자의 자연스러운 답을 오답 처리하지 않고 교정 전·후 결과를 모두 보존한다.
- 같은 학습 직후 교정은 회복 신호이며 독립 표본으로 중복 합산하지 않는다.
- 오답 해설은 원문(한자 후리가나 병기), 번역, 정답, 문법·의미, 함정, 유사 표현, 예문(후리가나 병기), 복습일, 다음 풀이 절차, 제출 전 체크를 포함한다. 문제나 답에 나오는 낯선 한자 단어 학습을 위해 해설 시 한자에 후리가나(괄호 또는 루비)를 항상 명시한다.

## 기록 원칙

- 사용자가 완료했다고 말하지 않은 활동을 완료로 추정하지 않는다.
- 점수·시간·복습 상태의 최종 원장은 `database/jlpt_learning.db`다.
- 사람이 읽는 기록은 `JLPT_STUDY_LOG.md`, 오답은 `JLPT_ERROR_NOTE.md`에 남긴다.
- 오답 복습은 D+1, D+3, D+7을 기본으로 한다.
- 정상 제출된 문항만 복습 완료 처리한다.
- 기존 미래 복습 일정을 임의로 삭제하거나 덮어쓰지 않는다.
- 학습시간은 확인된 시간만 더하며 중복을 피한다. 정수 분 밖의 초는 요약에 보존한다.
- 사용자의 명확한 하루 종료 신호 전에는 학습일을 닫지 않는다.

## 지표와 합격 가능성

- 유형별 정답률, 평균 풀이시간, 권장시간 대비 속도, 오답 원인, 지연 유지율을 누적한다.
- 표본이 적으면 범위와 신뢰도를 함께 제시한다.
- 합격 가능성은 공식 기관 점수가 아니라 내부 추정치임을 명확히 한다.
- 공식·준공식 종합 모의고사, 영역별 하한, 시간 압박, 지연 유지 데이터를 우선 반영한다.
- Anki 학습량이나 당일 즉시 재인만으로 합격 가능성을 크게 올리지 않는다.
- N2는 총점뿐 아니라 언어지식·독해·청해의 영역별 최저점 위험도 함께 본다.

## 일일 마감 보고

사용자가 하루 학습 종료를 명시하면 다음을 정리한다.

- 확인된 총 학습시간과 미확인 시간
- 완료한 강의·Anki·시험
- 전체 및 유형별 점수·속도
- 지연 유지와 당일 재인의 구분
- 확인된 강점, 반복 약점, 새 잠정 약점
- 오답 및 다음 복습 일정
- 합격 가능성 변화와 신뢰도
- 다음 학습 순서
- 홈페이지 동기화 결과
- 짧지만 솔직한 교사 의견

## 데이터·사이트 위치

- 홈페이지의 `記録済み学習時間` 아래 보조 문구는 날짜별 플래그와 관계없이 항상 `未計測の活動あり`로 표시한다. DB의 `has_untracked_activity` 원본 값은 변경하지 않는다.

- DB: `database/jlpt_learning.db`
- DB 설명: `JLPT_DATABASE.md`
- 학습 로그: `JLPT_STUDY_LOG.md`
- 오답노트: `JLPT_ERROR_NOTE.md`
- 보고 기준: `JLPT_REPORTING_STANDARD.md`
- 현재 상태: `HANDOFF.md`
- 원본 자료: `references/` — 읽기 전용
- 퀴즈: `quiz_templates/`
- 사이트: `jlpt-calendar-site/`
- 기본 홈페이지: `https://jlpt-study-calendar.pcw0611.workers.dev/`
- 기존 홈페이지: `https://jlpt-study-calendar.pcwww.chatgpt.site/`
- 동기화: `python jlpt-calendar-site/scripts/sync_learning_data.py YYYY-MM-DD`

## 보안·변경 제한

- `.sync-secret`, `.env`, API 키와 인증 정보를 채팅이나 Git에 노출하지 않는다.
- `references/` 아래 원본은 수정·이동·삭제하지 않는다.
- 기존 사이트 설정과 공개 주소를 보존한다.
- 사용자가 요청하지 않은 공식 점수·합격 확률·학습 완료 상태를 만들지 않는다.

## 세션 운영

- 새 세션은 `PROJECT_GUIDE.md`와 `HANDOFF.md`를 먼저 읽고 DB 최신 날짜를 확인한다.
- 긴 과거 대화를 다시 불러오지 않는다.
- `JLPT_STUDY_LOG.md`, `JLPT_ERROR_NOTE.md` 전체를 매번 읽지 않고 현재 작업에 필요한 날짜·시험·문항만 `rg`와 DB 쿼리로 가져온다.
- 긴 파일·도구 출력을 통째로 대화 컨텍스트에 넣지 않고 필요한 구간·필드만 읽는다.
- 이미 만든 퀴즈·사이트·이미지·배포를 먼저 확인해 재사용하고, 동일 결과물을 다시 생성하지 않는다.
- 정기적인 복습시험 제작은 기존 퀴즈 실행기와 데이터 교체를 기본으로 하며, 새 아티팩트 프로젝트 생성은 예외로 취급한다.
- `PROJECT_GUIDE.md`는 고정 원칙이 바뀔 때만 갱신한다.
- `HANDOFF.md`는 세션 이전 직전에 현재 진도·시간·시험·복습 큐·다음 행동으로 갱신한다.
- 컨텍스트가 커지면 사용량이 급증하기 전에 인계 문서를 갱신하고 새 프로젝트 작업으로 이동한다.
